build:
    main: main.go
    binary: unit
    goos:
        - windows
        - darwin
        - linux
    goarch:
        - amd64

dockers:
    - image: uneet/unit
      tag_templates:
          - '{{ .Tag }}'
          - 'v{{ .Major }}.{{ .Minor }}'
          - 'latest'
