build:
  env:
    - CGO_ENABLED=0

dockers:
    - image: uneet/unit
      tag_templates:
          - '{{ .Tag }}'
          - 'latest'
      extra_files:
          - sql/unit_create_new.sql
          - sql/unit_disable_existing.sql

archive:
    files:
      - sql/unit_create_new.sql
      - sql/unit_disable_existing.sql
